name: aida
description: AIDA - AI Personal Assistant (BSIT Mobile Programming Final Project)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  supabase_flutter: ^2.3.0
  http: ^1.2.0
  flutter_dotenv: ^5.1.0
  flutter_markdown: ^0.6.18
  google_fonts: ^6.1.0
  intl: ^0.19.0
  uuid: ^4.3.3
  shared_preferences: ^2.2.2
  flutter_markdown_plus: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env